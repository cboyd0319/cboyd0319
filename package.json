{
  "name": "cboyd0319-profile",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate": "node scripts/generate-signals.mjs",
    "test": "node scripts/test-utils.mjs",
    "check": "node --check scripts/generate-signals.mjs scripts/test-utils.mjs",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
